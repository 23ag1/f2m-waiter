# Дизайн-система waiter

Единый источник правды по отступам, типографике и переиспользованию. Цель —
консистентность на всех экранах (4pt-сетка + понятная шкала шрифтов + общие
примитивы вместо копипаста).

## 1. Отступы — сетка 4pt

Всё кратно **4px**. В Tailwind это **целые** классы. Половинные (`.5`) дают
2/6/10/14px — **вне сетки, не использовать**.

| Токен | px | Tailwind | Где |
|---|---|---|---|
| `0` | 0 | `p-0` | — |
| `xs` | 4 | `p-1 gap-1` | зазор иконка↔текст |
| `sm` | 8 | `p-2 gap-2` | плотные группы |
| `md` | 12 | `p-3 gap-3` | внутр. падинг карточки |
| `lg` | 16 | `p-4 gap-4` | боковые поля экрана, падинг шторки |
| `xl` | 20 | `p-5` | — |
| `2xl` | 24 | `p-6` | крупные секции |
| `3xl` | 32 | `p-8` | — |

**Запрещено:** `p-2.5 py-1.5 gap-1.5 mt-0.5 …` и произвольные `[10px]/[0.8rem]`.
Замены: `2.5→2 или 3`, `1.5→1 или 2`, `0.5→1` (или убрать), `3.5→3 или 4`.
Радиусы: `rounded-lg`(8) / `rounded-xl`(12) / `rounded-2xl`(16) / `rounded-full`.

## 2. Типографика

Шрифт — системный SF Pro (`--font-sans` в globals.css). Один шрифт на всё.
`--font-heading` дублирует `--font-sans` — можно удалить.

### Шкала (семантические роли, а не «на глаз»)

| Роль | Tailwind | px | Вес |
|---|---|---|---|
| Display (заголовок экрана) | `text-4xl` | 36 | `font-extrabold` |
| Title (заголовок шторки) | `text-xl` | 20 | `font-bold` |
| Headline (заголовок секции) | `text-lg` | 18 | `font-bold` |
| Body | `text-base` | 16 | `font-normal`/`medium` |
| **Body-sm (основной в POS)** | `text-sm` | 14 | `font-normal`/`medium` |
| Caption (лейблы, мета) | `text-xs` | 12 | `font-medium` |

**Минимум 12px.** `text-[10px]/[9px]/[8px]/[11px]/[15px]` — убрать, свести к шкале.
**Веса:** `normal`(400) текст · `medium`(500) акцент · `semibold`(600) кнопки/лейблы ·
`bold`(700) заголовки · `extrabold` только Display. `font-light` — не использовать.

## 3. Цвет (уже в globals.css — oklab токены)

Использовать семантические классы (`text-gray-900/500/400`, `bg-blue-500`,
`text-red-500`) консистентно: заголовок 900, вторичное 500, приглушённое 400,
акцент/ссылки blue-500/600, опасное red-500. Не плодить оттенки.

## 4. Переиспользование (сейчас плохо)

- **Шторки/модалки:** 21 инлайн-оверлей → использовать `components/ui/Sheet.tsx`
  (`Sheet` для форм, `ActionSheet` для списков действий).
- **Кнопки:** есть `components/ui/button.tsx` (cva) — использовать его, не сырые `<button>`.
- **Деньги:** формат `n.toLocaleString("ru-RU",{minimumFractionDigits:2})` повторяется —
  вынести в `shared` хелпер `formatMoney`.
- **Иконки:** повторяющиеся inline-svg (назад, ⋯, +, самолётик) → `components/ui/icons.tsx`.

## 5. Декомпозиция

Правило проекта: файл ≤800 строк, компонент ≤300. Нарушено:
`basket/[clientId]/page.tsx` **1714**, `new-order` **1398**, `menu` **468**.
Разбить basket на: `GuestRow`, `DishRow`, `MenuPanel`, `*Sheet`-компоненты,
хуки `useTableSession`, `useGuestBaskets`.

## 6. Архитектура: FSD — не внедрена

Сейчас: `app/`(роуты) + `components/`(ui,dashboard) + `lib/`(api). Это стандартный
Next, **не FSD** (нет слоёв shared/entities/features/widgets).

**Рекомендация (прагматичный FSD поверх Next App Router):**
```
src/
  app/                     # роуты Next (тонкие, только композиция)
  shared/  ui/ lib/ api/   # Sheet, Button, icons, formatMoney, api-клиент
  entities/ guest/ dish/ order/   # типы + карточки сущностей
  features/ add-dish/ send-order/ recommendations/ split-guest/
  widgets/ order-card/ menu/ orders-list/
```
Правило зависимостей: `app → widgets → features → entities → shared` (только вниз).
Миграцию делать **поэтапно** (по одному слайсу, с визуальной проверкой экрана),
не разом — риск сломать без тестов.

## 7. План раскатки

1. ✅ Токены/шкала (этот файл) + `Sheet`/`ActionSheet`.
2. Заменить 21 инлайн-оверлей на `Sheet`/`ActionSheet` (по экрану).
3. Прогнать 4pt-замену `.5`/`[Npx]` и свести шрифты к шкале — **поэкранно, со скрином**.
4. Вынести `formatMoney`, `icons`, применить `Button`.
5. Разбить бог-компоненты; при желании — миграция на FSD-слои.
